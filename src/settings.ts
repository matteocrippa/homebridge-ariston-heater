/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 * Must match `pluginAlias` in config.schema.json
 */
export const PLATFORM_NAME = 'AristonHeater';

/**
 * This must match the name of your plugin as defined in package.json `name`
 */
export const PLUGIN_NAME = 'homebridge-ariston-heater';
