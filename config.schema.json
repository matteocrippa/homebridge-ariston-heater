{
  "pluginAlias": "AristonHeater",
  "pluginType": "accessory",
  "headerDisplay": "Ariston NET Velis/Lydos water heater. Uses cloud API to read/set temperature and power.",
  "footerDisplay": "Tip: If the device behaves oddly after changes, remove ariston-cache.json from the Homebridge storage path to force a re-detection of the variant.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Ariston Heater"
      },
      "username": {
        "title": "Ariston Username (Email)",
        "type": "string"
      },
      "password": {
        "title": "Ariston Password",
        "type": "string",
        "format": "password"
      },
      "gateway": {
        "title": "Plant ID (optional)",
        "type": "string",
        "description": "If empty, the plugin will auto-discover the first available Velis/Lydos plant."
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "number",
        "default": 30,
        "minimum": 15,
        "description": "How often to refresh state from the cloud. Minimum 15 seconds."
      },
      "minTemp": {
        "title": "Minimum Target Temperature (°C)",
        "type": "number",
        "default": 35,
        "minimum": 1,
        "maximum": 95
      },
      "maxTemp": {
        "title": "Maximum Target Temperature (°C)",
        "type": "number",
        "default": 55,
        "minimum": 2,
        "maximum": 95
      },
      
      "debug": {
        "title": "Enable Debug Logging",
        "type": "boolean",
        "default": false
      }
    },
    "required": ["name", "username", "password"]
  }
}
